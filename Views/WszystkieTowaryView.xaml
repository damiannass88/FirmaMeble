<UserControl x:Class="FirmaMeble.Views.WszystkieTowaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="810" Background="Azure">
        <DataGrid  AutoGenerateColumns="False"  ItemsSource="{Binding  TowaryList}">
            <DataGrid.Columns>
                <DataGridTextColumn  x:Name="id"  Binding="{Binding  Path=id}" Header="Nr"/>
                <DataGridTextColumn  x:Name="kod"  Binding="{Binding  Path=kod}" Header="Kod"/>
                <DataGridTextColumn  x:Name="nazwa"  Binding="{Binding  Path=nazwa}" Header="Nazwa"/>
                <DataGridTextColumn  x:Name="cena"  Binding="{Binding  Path=cena}" Header="Cena"/>
                <DataGridTextColumn  x:Name="marza"  Binding="{Binding  Path=marza}" Header="Marza"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
