<UserControl x:Class="FirmaMeble.Views.WszystkieFakturyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id Faktury" Binding="{Binding IdFaktury}"/>
                <DataGridTextColumn Header="Numer" Binding="{Binding Numer}"/>
                <DataGridTextColumn Header="Data wystawienia" Binding="{Binding DataWystawienia}"/>
                <DataGridTextColumn Header="Nazwa Kontrhenta" Binding="{Binding KontrahentNazwa}"/>
                <DataGridTextColumn Header="NIP" Binding="{Binding KontrahentNIP}"/>
                <DataGridTextColumn Header="AdresDbSet" Binding="{Binding KontrahentAdres}"/>
                <DataGridTextColumn Header="Termin Platnosci" Binding="{Binding TerminPlatnosci}"/>
                <DataGridTextColumn Header="Sposob platnosci" Binding="{Binding SposobPlatnosciNazwa}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
